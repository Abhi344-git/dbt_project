version: 2

sources:
  - name: DBT_Citi_1
    description: "Transaction source table."
    database: SN_CITI_1
    schema: landing

    tables:
      - name: F_TRANS_L
        description: "Source transaction table."


models:
  - name: f_transaction_bronze
    description: >
      Bronze layer table storing raw transaction data loaded from
      SN_CITI_1.LANDING.F_TRANS_L with audit columns.

    config:
      database: DBT_CITI_1
      schema: F_TRANSACTION